services:
  backend:
    build: ./app/backend
    image: yolov8-backend
    ports:
      - "5001:5001"

  frontend: 
    build: ./app/frontend
    ports: 
      - "3000:80"
    depends_on:
      - backend
      